CREATE MATERIALIZED VIEW MV_GENESIS_CONJ AS
SELECT a.NR_CONJUNTO_ANEEL COD_ID,
'5697' DIST,
a.NM_CONJUNTO_CNS NOM,
1 SIST_INTE,
0 SIST_SUBT,
cast( null as varchar2(255) ) DESCR,
a.GEOMETRY
FROM VG_CONJUNTO_ANEEL a;